<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springpowpow.mapper.CommentMapper">

    <!-- 댓글 추가 -->
    <insert id="insertComment" parameterType="CommentVO">
        INSERT INTO TBL_COMMENT (ID, MEMBER_ID, POST_ID, COMMENT_CONTENT)
        VALUES (SEQ_COMMENT.NEXTVAL, #{memberId}, #{postId}, #{commentContent})
    </insert>

    <!-- 댓글 삭제 -->
    <delete id="deleteComment" parameterType="Long">
        DELETE FROM TBL_COMMENT
        WHERE ID = #{id}
    </delete>

    <!-- 댓글 리스트 -->
    <select id="selectAllComment" parameterType="Long" resultType="CommentVO">
        SELECT ID, MEMBER_ID, POST_ID, COMMENT_CONTENT
        FROM TBL_COMMENT
        WHERE POST_ID = #{postId}
    </select>

</mapper>